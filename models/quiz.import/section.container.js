define(['models/quiz.import/section', 'models/quiz.import/question'], function(SectionModel, QuestionModel){
    var SectionContainer = function(){
        this.addSection = function(){

        }

        //by global position
        this.getQuestionAt = function(position){

        }
    }

    return SectionContainer;
});